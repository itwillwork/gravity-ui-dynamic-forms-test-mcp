{
  "name": "gravity-ui-dynamic-forms-mcp",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/bundle.js",
  "bin": {
    "gravity-ui-dynamic-forms-mcp": "./dist/bundle.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc && chmod +x dist/index.js",
    "build:bundle": "rm -rf dist && npm run build && node scripts/create-bundle.js",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "esbuild": "^0.19.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
